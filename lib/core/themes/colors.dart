import 'package:flutter/material.dart';

class AppColors {
  static const scaffoldBackground = Color(0xFFfef9ef);
  static const accent = Color(0xFF283618);
  static const border = Color(0xFF606c38);
  static const lightBackground = Color(0xFFDDA15E);

  static final plannedColor = Colors.red.withOpacity(0.3);
  static const executingColor = AppColors.lightBackground;
  static final doneColor = Colors.green.withOpacity(0.5);
}
